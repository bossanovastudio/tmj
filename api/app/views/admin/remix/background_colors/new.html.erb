<section id="cards" style="margin-top: 100px;">
    <div class="content">
        <%= simple_form_for [:admin, @background] do |f| %>
          <%= f.input :color, label: 'Color', error: 'Color is mandatory, please specify one' %>
          <%= f.button :submit %>
        <% end %>
    </div>
</section>
